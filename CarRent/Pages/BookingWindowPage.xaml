<Page x:Class="CarRent.Pages.BookingWindowPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CarRent.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Бронирование">

    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>

        <!-- Дата начала -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата начала:" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <DatePicker x:Name="StartDate" Grid.Row="1" Grid.Column="1" 
                    Margin="0,0,0,10"
                    SelectedDateChanged="DatePicker_SelectedDateChanged"/>

        <!-- Дата окончания -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата окончания:" 
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <DatePicker x:Name="EndDate" Grid.Row="2" Grid.Column="1" 
                    Margin="0,0,0,10"
                    SelectedDateChanged="DatePicker_SelectedDateChanged"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="1" VerticalAlignment="Center">
        <!-- Количество дней -->
        <TextBlock Text="Количество дней:" Margin="0,0,10,10"/>
        <TextBlock x:Name="DaysCount" Text="0" Margin="0,0,10,10"/>

        <!-- Итоговая стоимость -->
       
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <TextBlock Text="Итоговая стоимость: "/>
                <TextBlock x:Name="TotalCost" Text="0"/>
                <TextBlock Text=" руб."/>
            </StackPanel>
        </StackPanel>    

        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,0">
            <Button Content="Рассчитать" 
                    Click="CalculateButton_Click"
                    Padding="20,10" 
                    Margin="0,0,10,0"
                    Width="100"/>
            <Button Content="Забронировать" 
                    Click="BookButton_Click"
                    Padding="20,10" 
                    Margin="10,0,0,0"
                    Width="120"/>
        </StackPanel>

    </Grid>
</Page>
